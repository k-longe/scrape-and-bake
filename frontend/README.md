# Frontend

This frontend is the static UI package for `scrape-and-bake`.

For setup, regeneration, database seeding, fixture scrape outputs, and deployment notes, start with the repo root `README.md`.
